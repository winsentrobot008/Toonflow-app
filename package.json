{
  "name": "toonflow-root",
  "version": "1.0.0",
  "private": true,
  "description": "Toonflow - AI Short Drama Studio",
  "scripts": {
    "dev": "cd app && npx --no-install next dev",
    "build": "cd app && npx --no-install next build",
    "start": "cd app && npx --no-install next start",
    "lint": "cd app && npx --no-install next lint"
  }
}
